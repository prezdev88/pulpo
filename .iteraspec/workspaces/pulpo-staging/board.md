# Backlog Board: Pulpo Staging

## 🔴 To Do

## 🟡 In Progress

## 🟢 Done
- T01: Core Logic - Implement Git backend use cases (`getStatus`, `stageFiles`, `unstageFiles`, `commitChanges`, `getLiveDiff`).
- T02: Main & Preload - Expose new Git use cases via IPC.
- T03: UI/UX - Sidebar layout update (Navigation toggle, Staging View HTML structure, and CSS).
- T04: UI Integration - Wire up Staging view logic in `app.js` (Render status, handle stage/unstage clicks, live diffs).
- T05: UI Integration - Implement Commit execution and isolate draft message state per tab.
